<div id="header" class="flex navbar-fixed-top">
  <div id="header-logo">
    <%= image_tag "nagual_logo.svg" %>
  </div>
  <span class="flex-spacer"></span>
  <div id="header-menu">
    <span>
    <% if user_signed_in? %>
      Welcome <strong><%= current_user.email %></strong> | Help | <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
    <% else %>
      Welcome <strong>Anakin</strong> | Help
    <% end %>
    </span>
  </div>
</div>
