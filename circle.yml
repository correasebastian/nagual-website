machine:
  node:
    version:
      6.10.2

deployment:
  staging:
    branch: develop
    commands:
      - npm run deploy:staging
  prod:
    branch: aws
    commands:
      - npm run build
      - aws s3 sync dist https://s3.amazonaws.com/nagual-website/ --delete
