machine:
  timezone:
    America/New_York
  ruby:
    version: 2.3.1
general:
  branches:
    only:
      - master
dependencies:
  pre:
    - gem install bundler
test:
  override:
    - bundle exec rake
